#!/usr/bin/env bash
set -euo pipefail

echo "Skipping Gradle invocation: Expo project has no prebuilt Android artifacts in this repository."
echo "If you need native Android artifacts locally, run from this folder:"
echo "  npx expo prebuild --platform android"
exit 0
